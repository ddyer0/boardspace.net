
edited /etc/http/httpd.conf and conf.d/boardpace.net.conf to add port 4321

added .htaccess to games directories so they can be scanned.

(ticketed)
enable ssi for apache
upgrade mysql
install gcc and gdb
