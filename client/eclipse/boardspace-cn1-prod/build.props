build.classes.dir=${build.dir}/
# This directory is removed when the project is cleaned:
build.dir=bin
# Only compile against the classpath explicitly listed here:
dist.dir=dist
dist.jar=${dist.dir}/boardspace-cn1-prod.jar
build.test.classes.dir=${build.dir}/prod/classes
build.test.results.dir=${build.dir}/prod/results
test.src.dir=test
javac.test.classpath=./JavaSE.jar:\
    ${javac.classpath}:\
    ${build.classes.dir}
