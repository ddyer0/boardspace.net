package lib;

public interface UniversalConstants {
   
    /**
     * hidden offscreen windows are positioned far to the left of the main screen, so
     * widgets that are displayed on them are easily distinguishable from those on
     * the real window.
     */
    static final int HIDDEN_WINDOW_RIGHT = -2000;
  

}
