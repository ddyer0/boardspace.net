package bridge;

import java.awt.Component;

import javax.swing.JPanel;

import lib.MenuInterface;
// dummy class for standard java, does not change the frame size
public class FullscreenPanel extends JPanel 
{

	/**
	 * 
	 */
	private static final long serialVersionUID = 1L;
	public void show(MenuInterface m,int x,int y)
	{
		m.show(this, x, y);
	}
	public void add(MenuInterface menu)
	{	add((Component)menu);
	}

}
